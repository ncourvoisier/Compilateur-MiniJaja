class C {
    int x = (0+(2*3));
    boolean t[8];
    boolean y = (true && (false || (1 == 2)));

    main  {
        final int max = 8;
        int current = 0;
        while(max>current){
            t[current] = false;
            current++;
        };
        x=-2;
        while(0>x) {
            write("coucou ");
            writeln(x);
            x++;
        };
    }
}